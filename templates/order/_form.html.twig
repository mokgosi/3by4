{% form_theme form 'bootstrap_5_layout.html.twig' %}

{{ form_start(form, {'attr': {'novalidate':'novalidate'}}) }}

    {{ form_row(form.country) }}
    {{ form_row(form.kit) }}

    {{ form_row(form.patient.email, {'attr': {'class': 'typeahead'}}) }}
    {{ form_row(form.patient.firstName, {'attr': {'class': 'typeahead'}}) }}
    {{ form_row(form.patient.lastName, {'attr': {'class': 'typeahead'}}) }}

    {{ form_row(form.paid) }}

{{ form_end(form) }}


